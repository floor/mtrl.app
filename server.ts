// server.ts
// Entry point for the server
import server from "./server/index.ts";

// The server is already running at this point
// This file exists to provide a simple entry point
console.log(`Server running at ${server.port}`);